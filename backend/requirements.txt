# Backend dependencies
fastapi==0.111.0         # Web framework — handles HTTP requests
uvicorn==0.30.1          # Server that runs FastAPI
anthropic==0.28.0        # Claude API SDK
qdrant-client==1.9.1     # Talks to our vector database
sqlalchemy==2.0.30       # Talks to PostgreSQL
asyncpg==0.29.0          # Async PostgreSQL driver
python-dotenv==1.0.1     # Loads .env variables
httpx==0.27.0            # HTTP client (for calling GitHub API)
tiktoken==0.7.0          # Counts tokens (important for chunking)
sentence-transformers==3.0.1  # Creates embeddings locally (free option)
pydantic==2.7.1          # Data validation
